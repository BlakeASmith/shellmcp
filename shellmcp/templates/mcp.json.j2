{
  "mcpServers": {
    "{{ config.server.name }}": {
      "command": "{{ server_directory }}/venv/bin/python",
      "args": ["{{ server_script_path }}"],
      "cwd": "{{ server_directory }}",
      "env": {
        "PYTHONPATH": "{{ server_directory }}"
      }
    }
  }
}